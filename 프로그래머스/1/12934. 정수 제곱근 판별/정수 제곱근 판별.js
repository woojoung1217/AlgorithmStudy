function solution(n) {
    var answer = 0;
    let a = Math.sqrt(n)
    if(Number.isInteger(a) === true) {
        return (a+1) ** 2
    } else if (Number.isInteger(a) === false) {
        return -1
        
    }
    
}

// x = n / 2  2a = x 